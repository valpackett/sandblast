{
	"ipv4": "192.168.1.99",
	"hostname": "firefox.jail",
	"process": "#!/bin/sh\npkg install firefox gnome-themes-standard; pw userdel firefox; pw useradd -n firefox -d /home/firefox -s /bin/sh; mkdir -p /home/firefox; chown firefox /home/firefox",
	"plugins": [
		{
			"name": "mount_dir",
			"options": { "source": "/var/worlds/10.1-RELEASE", "mode": "ro" }
		},
		{
			"name": "mount_dir",
			"options": { "source": "/var/containers/firefox", "mode": "rw" }
		},
		"mount_dev",
		{
			"name": "netif_alias",
			"options": { "iface": "vtnet0" }
		}
	]
}
